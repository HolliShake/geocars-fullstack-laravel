VITE_APP_API_URL=http://localhost:8000
VITE_APP_STRIPE_PK=
# Stripe checkout: leave empty in local dev to use Vite proxy /stripe-service → port 5000.
# Set a full URL (e.g. https://api.example.com/stripe) in production.
VITE_APP_STRIPE_CHECKOUT_URL=
# When the app runs in Docker, point the proxy at the compose service name:
# VITE_STRIPE_PROXY_TARGET=http://stripe:5000